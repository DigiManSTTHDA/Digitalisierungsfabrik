{
  "openapi": "3.1.0",
  "info": {
    "title": "Digitalisierungsfabrik API",
    "description": "AI-guided business process elicitation system. Placeholder spec — replaced at end of Epic 05.",
    "version": "0.1.0"
  },
  "paths": {
    "/health": {
      "get": {
        "summary": "Health Check",
        "description": "Liveness probe — returns ok when the server is running.",
        "operationId": "health_check_health_get",
        "tags": ["meta"],
        "responses": {
          "200": {
            "description": "Server is running",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "HealthResponse": {
        "type": "object",
        "title": "HealthResponse",
        "properties": {
          "status": {
            "type": "string",
            "title": "Status"
          }
        },
        "required": ["status"]
      }
    }
  }
}
