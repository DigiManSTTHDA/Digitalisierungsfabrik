/**
 * Root layout component — placeholder for Epic 06.
 *
 * The full split-pane layout (Chat | Artifact | Debug) is implemented in Epic 06.
 * For now this renders a minimal placeholder so the Vite dev server is visibly running.
 */
function App() {
  return (
    <div style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <h1>Digitalisierungsfabrik</h1>
      <p>Prototyp – wird in Epic 06 vollständig implementiert.</p>
    </div>
  );
}

export default App;
